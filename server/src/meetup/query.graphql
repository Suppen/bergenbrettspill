query Events($urlname: String!) {
  groupByUrlname(urlname: $urlname) {
    events(sort: ASC) {
      edges {
        node {
          id
          title
          eventUrl
          dateTime
          maxTickets
        }
      }
    }
  }
}

